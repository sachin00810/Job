export interface User { id?: string; }
export interface Job { id?: string; }
export interface Company { id?: string; }
export interface Room { id?: string; }
export interface Application { id?: string; }